
name: Docker Build and Push

on:
    push:
        branches:
            - main
jobs:
    build:
        runs-on: windows-latest
        steps:
            - uses: actions/checkout@v4
            - name: Docker Hub Login
              run: echo "mypassword" |  docker login -u codewithfaraz -password-stdin
